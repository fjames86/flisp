
#ifndef ENV_H
#define ENV_H

/* environment contains a pair of the toplevel (special) bindings and an assoc list of locals */
#if 0
typedef struct {
	hash_table *specials;
	cell *lexical;
} env;
#endif

#endif
